# Power off the system
sudo poweroff

# Restart the system
sudo reboot

# Shutdown with delay (e.g., 5 minutes)
sudo shutdown +5

# Shutdown immediately
sudo shutdown now

# Log out of the current session
gnome-session-quit --logout --no-prompt

# Reboot the system immediately
sudo reboot

# Check system uptime
uptime

# View system information
uname -a
